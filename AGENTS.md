# arcade-sdk

## Commands

- `npm install` — install dev dependencies (TypeScript only)
- `npm run typecheck` — type-check `types/arcade.d.ts` (`tsc --noEmit`)

There is no build step. `arcade.js` is hand-written ES2019 and ships as-is.
